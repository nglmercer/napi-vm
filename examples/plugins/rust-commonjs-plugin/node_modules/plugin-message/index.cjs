const fs = require("node:fs");
const path = require("node:path");

module.exports = (context) => {
  const config = JSON.parse(fs.readFileSync("./config.json", "utf8"));
  return {
    message: `${config.greeting}, ${config.name}!`,
    plugin: context.name,
    package: path.basename(__dirname),
  };
};
